{
  "buildCommand": "npm --prefix my-vite-app install && npm --prefix my-vite-app run build",
  "outputDirectory": "my-vite-app/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
